Install these Python libraries by running this commands in your terminal:
pip install streamlit pandas plotly openpyxl

then,

pip install statsmodels

Save the app.py file on your pc

On terminal navigate to the app.py then run this code: streamlit run app.py

Open a csv file and click raw. Copy the URL and paste it in "Paste CSV File URL: " on AI Pattern Finder.
