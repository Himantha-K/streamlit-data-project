Install the Python libraries by running this command in your terminal: pip install streamlit pandas plotly openpyxl
Save the app.py file on your pc
On terminal navigate to the app.py then run this code: streamlit run app.py
